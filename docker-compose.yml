services:
  app:
    build: .
    ports:
      - "3004:3000"
    volumes:
      - ./data:/app/data
    environment:
      - CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
      - DATABASE_PATH=/app/data/syndicus.db
    network_mode: bridge
